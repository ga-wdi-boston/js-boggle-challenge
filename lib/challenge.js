'use strict';

// Requirements
// Map row/column coordinates onto an array index (i.e. represent a square
// grid using a simple array). Use this mapping to return (potential) words
// from letters in the grid.
// boggle word game, 4x4 grid, letter dice,
// dice random value of six values
// that takes a tray and a list of coordinate pairs and returns a string
// representing the concatenation of the letters at each of the coordinates.

// tray is just an array of length 16 with each element a single letter string
// list of coordinate pairs is an array with length <= 4 and each element is
// an array of length 2. The sub-arrays contain two integers >= 0 and <= 3

// Create tray generator, manual or make function to generate
// Coordinate pair lister
// look at tray, pull coordinates, output coordinate string
//

let tray = {
  position1: '',
  position1: '',
  position1: '',
  position1: '',
};

const boggle = function boggle() {
  let x = '';
  let y = '';
  if (i = 0; i < 4; i++){
    x++;
  }
};

const bigBoggle = function bigBoggle() {};

const superBigBoggle = function superBigBoggle() {};

module.exports = {
  boggle,
  bigBoggle,
  superBigBoggle,
};
