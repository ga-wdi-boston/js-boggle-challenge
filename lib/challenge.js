'use strict';

let tray = [];
for (let i = 0; i < 16; i ++)
{
    tray = tray[i];
}

let locp = [];
for (let i = 0; i < 4; i ++)
{
    locp = locp[i];
}

let sa = [];
for (let i = 0; i < 2; i ++)
{
    sa = sa[i];
}

let saint() = function getRandomArbitrary(1, 3) {
  return Math.random() * (max - min) + min;
}





const boggle = function boggle() {};

const bigBoggle = function bigBoggle() {};

const superBigBoggle = function superBigBoggle() {};

module.exports = {
  boggle,
  bigBoggle,
  superBigBoggle,
};
